import { Box } from "@mui/material";

export default function Tasks() {
  return (
    <Box
      sx={{
        display: "flex",
        height: 1,
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "column",
      }}
    >
      <h1>Tasks Page</h1>
    </Box>
  );
}
